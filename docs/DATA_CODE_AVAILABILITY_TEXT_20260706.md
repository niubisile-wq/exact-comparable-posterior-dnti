# Data And Code Availability Text

Use these texts in the submission system if separate boxes are provided. Keep them consistent with the manuscript.

Do not invent a DOI, repository name, or public link. Use the public-DOI wording only after the repository is actually public and the DOI resolves outside the author account.

## Current Submission Version: No Public DOI Yet

### Data Availability

The frozen result manifests, derived figure data, and result-traceability map supporting the findings of this study are retained in the submission package and can be provided to editors and reviewers during peer review upon reasonable request. Reused public network and measurement sources are documented in the external-data access notes. Upon acceptance or before publication, the frozen result manifests, figure source data, traceability map, and derived support files will be deposited in a public archival repository, and the repository link and persistent identifier will be added to the article record.

### Code Availability

The code used to generate topology libraries, train and evaluate posterior models, run baseline and stress-test experiments, compute statistical tests, and regenerate figures is retained in the submission package and can be provided to editors and reviewers during peer review upon reasonable request. A repository-ready code bundle has been organized with core scripts, experiment drivers, frozen result manifests, external-data access notes, a reviewer-facing reproduction guide, a data dictionary, and a result-traceability map. Upon acceptance or before publication, the public repository link will be added after archival upload.

## Stronger Version: Public Repository DOI Created Before Submission

Use this only after replacing `[DOI]`, `[URL]`, `[Repository]`, and `[version]` with real values.

### Data Availability

The frozen result manifests, derived figure source data, result-traceability map, and external-data access notes supporting the findings of this study are available in [Repository] at [DOI] ([URL]), version [version]. Reused public network and measurement sources are documented in the deposited external-data access notes together with the source, version or access date, and use restrictions where applicable.

### Code Availability

The code used to generate topology libraries, train and evaluate posterior models, run baseline and stress-test experiments, compute statistical tests, and regenerate figures is available in [Repository] at [DOI] ([URL]), version [version]. The archived release contains core scripts, experiment drivers, frozen result manifests, external-data access notes, a reviewer-facing reproduction guide, a data dictionary, and a result-traceability map.

## Suggested Dataset/Software Reference After DOI Exists

Replace all placeholders before use.

```text
[dataset] Liu, Z. and Xiong, W. ([Year]). Exact-comparable posterior contract for decision-ready distribution-network topology identification: code, figure source data, and result manifests, version [version]. [Repository]. [DOI].
```

## If The Submission System Offers Fixed Choices

Recommended data choice:
Data are available from the authors upon reasonable request.

Recommended code choice:
Code is available from the authors upon reasonable request and will be deposited in a public repository upon acceptance or before publication.

Do not choose:
No data available.

Do not choose:
Data are already publicly available with DOI, unless a real DOI or repository link has already been created.

## Unresolved Author Action

Before final publication, create an archival repository record if the journal asks for a public link. If you create a public DOI before first submission, replace the current request-based text with the stronger public-DOI version above.
